1. **视频代码同步方式**: 方案1(自动同步) / 方案2(手动切换) / 方案3(混合)?

   ​	混合的，可以自动切换也可以手动切换，如何用户不点击就自动的进行切换。

2. **代码展示模式**: 模式1(完整高亮) / 模式2(渐进累积) / 模式3(卡片式)?

   ​	我们代码展示是类似于一个编辑器类型的，然后全部代码都显示,但当前讲解的部分高亮

3. **主要支持的视频平台**: YouTube? Bilibili? 本地文件? 全部?

   ​	目前是支持任何的api支持的，youtube，哔哩哔哩，tiktok

4. **时间轴映射生成**: 方案A(两阶段) / 方案B(一次性)?

   选一个容易实现的方式

5. **代码分段策略**: 语义分段(推荐) / 时间分段 / 行数分段?

   选一个容易实现的方式

   

6. **沙盒执行时机**: 手动执行(推荐) / 自动执行 / 实时预览?

   目前不打算做沙盒了，使用一个前端编辑器，能够执行代码，看到对应输出就行。

   

7. **是否需要缓存**: 是 / 否? 

   是

8. **是否需要数据库存储**: 是 / 否?

   是

9. 技术栈

   

10. 前端交互设计

    第一个页面的布局：

    整体采用**左右分栏+顶部导航+底部信息**的布局，风格简洁现代。以下是详细结构描述：  ### 1. 顶部全局导航栏（全宽度） - 左侧：返回、保存、复制等操作图标（简洁线条样式）。 - 中间：“Preview”（预览）/“Code”（代码）切换按钮。 - 右侧：功能图标（通知、设置等）+“Publish”（发布）按钮+“Share”（分享）按钮（蓝色背景）。  ### 2. 主体布局：左侧侧边栏 + 右侧主内容区 #### 左侧侧边栏（固定宽度，浅背景） - 顶部：品牌标识“Mauns”（黑色M图标+文字）+ 菜单展开按钮。 - 功能按钮区：  - “New Chat”（新对话，带加号图标+键盘快捷键标识）。  - “Video to Code”（视频转代码，带代码图标）。  - “Slides”（幻灯片，带幻灯片图标）。 - 底部：“History”（历史记录，带时钟图标）+ 用户信息（头像+邮箱）。  #### 右侧主内容区（自适应宽度，白色背景） - 顶部栏：  - 左侧：品牌标识“Mauns”（紫色M图标+文字）。  - 右侧：“Sign In”（登录，文字按钮）+ “Try for Free”（免费试用，紫色背景按钮）。 - 中间核心区域（居中对齐）：  - 状态标签：“AI Assistant Online”（蓝色浅背景，小圆点标识在线）。  - 主标题：“Hello, I'm Mauns”（黑紫渐变色字体，大号粗体）。  - 副标题：“An AI assistant that helps you create content, generate code, and make presentations”（灰色小字，居中）。 - 交互区域：  - 输入框：占位符“Type anything to start chatting...”，外框带蓝紫渐变色发光效果，右侧带向上箭头发送图标。  - 功能快捷按钮区（输入框下方）：    - “Video to Code”（代码图标）    - “Deep Research”（闪电图标）    - “Social Post”（魔法棒图标）    - “Slides”（幻灯片图标）    - “To do list”（列表图标）    （按钮为浅灰边框+文字，圆角样式）  ### 3. 底部信息栏（全宽度，主内容区底部） - 文字链接：“Terms of Service”（服务条款）、“Privacy Policy”（隐私政策）、“Help Center”（帮助中心）。 - 右侧：帮助图标（问号圆形按钮）。  ### 样式要点 - 配色：主色调为黑、白、灰+蓝紫渐变色（品牌色、按钮、输入框边框）。 - 风格：扁平化+轻微发光效果（输入框），圆角元素（按钮、输入框），简洁无多余装饰。 

     

    第二个页面的布局我们还没考虑好，等会会给你：

    

    

11. 交互流程细节

    代码显示是**流式**(像ChatGPT那样逐字出现)。

    

12. 前端展示的信息层级

    当我点击vidio to code的时候，我再放一个视频的url上去，就会跳转到第二个界面，

13. prompt

    *# 伪代码示例* prompt = f""" 你是一个Python教学助手。 根据以下视频教学内容,生成可运行的Python代码: 【视频信息】 标题: {video_title} 时长: {duration} 【教学内容总结】 {summary} 【详细字幕(如果有)】 {subtitle} 【要求】 1. 生成完整可运行的代码 2. 包含详细的中文注释(解释每个关键步骤) 3. 使用Python 3.9+语法 4. 如果需要第三方库,在代码顶部用注释列出 5. 包含必要的错误处理 6. 代码风格遵循PEP8 【输出格式】 直接输出Python代码,不要用markdown代码块包裹 """，这个还不行，还要调整

    

14.  **生成代码的范围** 场景举例:

    一个20分钟的Python爬虫教程，最好使用一个简单的方法，ai编程好实现的方法，时间目前并不是考虑的范围

15. **代码的组织形式** 生成的代码是:

    **代码块 + 说明**: 像教程一样,分成多个代码块,每个块前有文字说明





# 3.需求文档

# Mora Video-to-Code 开发实施清单

## 🎯 项目总览

**MVP****目标**: 实现视频URL → 代码生成 → 视频同步演示 → 前端执行的完整流程

## **技术栈**:

- 前端: 已完成基本的交互逻辑+ Element Plus + Monaco Editor + Pyodide
- 后端: FastAPI + PostgreSQL + Redis + python
- 第三方: BibiGPT API + Deepseek

## 前端的交互模式：

**Home 页面**

```Plain
用户输入视频 URL
     ↓
点击 "Video to Code" → 调用 POST /api/session 创建任务
点击其他按钮 → 类似于Toast "即将上线"
     ↓
拿到 sessionId → 跳转 /workspace?session={sessionId}
```

第二个页面：

前端交互逻辑文档：[mora前端交互逻辑](https://ai.feishu.cn/wiki/XyjzwiEamikhShkzUi9cnBmUnMd)

**Workspace 页面**

```Plain
进入页面
     ↓
从 URL 取 sessionId
     ↓
调用 GET /api/session/{sessionId} 获取 videoUrl + 当前状态
     ↓
┌──────────────────────────────────────────────────────┐
│                                                      │
│  ┌─────────────┐    ┌─────────────────────────────┐ │
│  │             │    │  Generated Code             │ │
│  │   Video     │    │  ← SSE 流式接收              │ │
│  │   Player    │    │  [复制] [下载]              │ │
│  │             │    └─────────────────────────────┘ │
│  └─────────────┘    ┌─────────────────────────────┐ │
│  ┌─────────────┐    │  Execution Result           │ │
│  │ Agent       │    │  ← 代码生成完成后自动执行    │ │
│  │ Thought     │    │  [重新运行]                 │ │
│  │ ← SSE 流式  │    └─────────────────────────────┘ │
│  └─────────────┘                                    │
│                                                      │
│  [Chat 按钮] → 点击弹出 "即将上线"                    │
└──────────────────────────────────────────────────────┘
```

**数据流（单向 SSE）**

```Plain
前端建立 SSE 连接: GET /api/session/{sessionId}/stream
     ↓
后端推送事件:
  { type: "thought", content: "正在提取音频..." }
  { type: "thought", content: "识别到函数定义..." }
  { type: "code", content: "def extract..." }      // 可分片推送
  { type: "code_done" }                            // 代码生成完成
  { type: "execution", content: "输出结果..." }
  { type: "done" }                                 // 全部完成
     ↓
前端根据 type 更新对应模块
```

**状态机（前端）**

```Plain
idle → loading → processing → completed / error
                    ↓
            可随时刷新页面，根据 sessionId 恢复状态
```

**需要的** **API****（3 个）**

| 方法 | 路径                     | 作用                     |
| ---- | ------------------------ | ------------------------ |
| POST | /api/session             | 创建任务，返回 sessionId |
| GET  | /api/session/{id}        | 获取任务状态 & videoUrl  |
| GET  | /api/session/{id}/stream | SSE 流式推送             |

## 后端的交互逻辑：

后端整体运行的逻辑如下：

​    A[用户打开首页] --> B[输入视频URL]

​    B --> C{URL验证}

​    C -->|有效| D[跳转到视频处理页]

​    C -->|无效| B

​    D --> E[调用BibiGPT API获取字幕]

​    E --> F[AI生成代码]

​    F --> G[AI生成时间轴映射]

​    G --> H[跳转到视频代码页]

​    H --> I[视频播放 + 代码同步高亮]

​    I --> J{用户操作}

​    J -->|编辑代码| K[Monaco编辑器]

​    J -->|运行代码| L[Pyodide执行]

​    J -->|AI对话| M[优化建议]

​    K --> L

​    M --> K

​    L --> N[显示执行结果]

1.调用BibiGPT API获取字幕详细细节：

调用方式：

curl --request GET \

  --url 'https://api.bibigpt.co/api/v1/getSubtitle?enabledSpeaker=true' \

  --header 'Authorization: Bearer <token>'

返回的json格式

## getSubtitle

```Plain
{
  "success": true,
  "id": "<string>",
  "service": "<string>",
  "sourceUrl": "<string>",
  "htmlUrl": "<string>",
  "costDuration": 123,
  "remainingTime": 123,
  "summary": "<string>",
  "detail": {
    "id": "<string>",
    "url": "<string>",
    "type": "bilibili",
    "title": "<string>",
    "duration": 123,
    "summary": "<string>",
    "dbId": "<string>",
    "embedId": "<string>",
    "pageId": "<string>",
    "rawLang": "<string>",
    "translationUrls": [
      {
        "code": "<string>",
        "url": "<string>"
      }
    ],
    "audioUrl": "<string>",
    "playUrl": "<string>",
    "cover": "<string>",
    "author": "<string>",
    "authorId": "<string>",
    "publishedDate": "<unknown>",
    "subtitlesArray": [
      {
        "startTime": 123,
        "end": 123,
        "text": "<string>",
        "index": 123,
        "words": [
          {
            "start": 123,
            "end": 123,
            "text": "<string>",
            "punctuation": "<string>"
          }
        ],
        "speaker_id": 123,
        "_isGrouped": true,
        "_originalSubtitles": [
          "<unknown>"
        ]
      }
    ],
    "descriptionText": "<string>",
    "contentText": "<string>",
    "aiImages": [
      {
        "startTime": 123,
        "endTime": 123,
        "prompt": "<string>",
        "image": "<string>"
      }
    ],
    "chapters": [
      {
        "from": 123,
        "to": 123,
        "content": "<string>",
        "type": 123,
        "imgUrl": "<string>"
      }
    ],
    "local_path": "<string>",
    "status": "<string>"
  }
}
```

2.AI生成代码：

目标：

3.AI生成时间轴映射：

可以写一个对应的函数，用来进行，AI分析字幕,生成时间戳与代码行号的映射表：

{

  "timeline": [

​    {

​      "startTime": "00:00",

​      "endTime": "02:30",

​      "description": "介绍和环境准备",

​      "codeLines": null  // 这段没有代码

​    },

​    {

​      "startTime": "02:30",

​      "endTime": "05:15",

​      "description": "安装依赖和导入库",

​      "codeLines": "1-10",

​      "code": "import requests\nimport json..."

​    },

​    {

​      "startTime": "05:15",

​      "endTime": "08:00",

​      "description": "发送HTTP请求",

​      "codeLines": "15-25",

​      "code": "response = requests.get(url)..."

​    }

  ]

}

4.跳转到视频代码页：

5.视频播放 + 代码同步高亮：

6.时间轴的映射

7.编辑代码| K[Monaco编辑器]：

可以使用前端编辑器的框架

8.|运行代码| L[Pyodide执行]：

Pyodide是Python的WebAssembly编译版本,可在浏览器中运行Python代码。

优先级的实现：分步骤实现

### 实现的步骤；

1.第一步实现点击video to code并输入url后，能够进行调用BibiGPT API获取字幕详细细节，并进行流式输出

2.实现根据获取的字幕信息生成对应的代码，并输出一个时间轴映射，也是流式输出。

3.根据时间轴引射一边看视频一边能够根据视频内容展示代码，并运行代码

4。运行代码并跑通





1. 功能优先级排序（从高到低):
   -  视频播放 + 前端编辑器代码同步显示
   - 代码在线编辑
   -  前端编辑器运行代码
   -  AI对话优化代码











